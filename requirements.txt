fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiohttp>=3.9.0
pydantic>=2.7.0
python-dotenv>=1.0.0
# OnPage agent
beautifulsoup4>=4.12.0
# Content Gap agent
nltk>=3.8.0
# (newspaper3k is optional — content_gap_agent uses aiohttp+bs4 directly)

# LangGraph + async graph execution
langgraph>=0.3.18
sse-starlette>=2.1.0
asyncpg>=0.29.0
